// Silhouette wordmark — small, restrained, serif italic.
// Used at the top of each state view (welcome, thinking, insight, clarify).

export function Wordmark() {
  return (
    <h1 className="text-sil-text text-xl tracking-wide">
      <span className="font-serif italic">Silhouette</span>
    </h1>
  );
}
